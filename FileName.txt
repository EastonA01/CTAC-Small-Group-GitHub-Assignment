git init
- The command "git init" initializes the repository in the local directory.

git add (Addition Arguments go here)
- This command prepares tracked files for a commit using additional arguments to specify location and files.

git push 
- This command "pushes" the new commit to github

git fetch

- This command "fetches" current additions without really "pulling" the information 